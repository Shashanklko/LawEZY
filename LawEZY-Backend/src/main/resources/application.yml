server:
  port: ${PORT:8080}

spring:
  application:
    name: LawEZY-Backend

  # MySQL Configuration
  datasource:
    url: ${TIDB_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/lawezy?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true}
    username: ${TIDB_USER:2kwpxkd5yC95e7H.root}
    password: ${TIDB_PASSWORD:OhoXmvQCMLN1k4vR}
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA / Hibernate Configurations
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL:update}
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  # MongoDB Configuration
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb+srv://lawezy2025:Shashank321@lawezy.gfeffjz.mongodb.net/lawezy_main?appName=Lawezy}

  # 📧 INSTITUTIONAL EMAIL INFRASTRUCTURE
  gmail:
    client-id: ${GMAIL_CLIENT_ID:placeholder}
    client-secret: ${GMAIL_CLIENT_SECRET:placeholder}
    refresh-token: ${GMAIL_REFRESH_TOKEN:placeholder}
    user-email: ${GMAIL_USER_EMAIL:lawezy2025@gmail.com}

  # 🏛️ SPRING AI ELITE GEMINI CONFIG
  ai:
    google:
      genai:
        api-key: ${GOOGLE_AI_API_KEY:placeholder}
        chat:
          options:
            model: gemini-1.5-flash

# Custom JWT Application Properties
jwt:
  secret: ${JWT_SECRET:lawezy-institutional-secret-key-2025-grid-secure}
  expirationMs: ${JWT_EXPIRATION_MS:86400000}

# Custom Application Properties
app:
  internal:
    secret: ${INTERNAL_SECRET:lawezy-institutional-internal-grid-secret-2025}
  messenger:
    url: ${MESSENGER_URL:https://lawezy-messenger.onrender.com}
  cors:
    allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173,https://lawezy.onrender.com,https://lawezy-sigma.vercel.app}
  seed:
    enabled: ${SEED_DATA:true}
    admin-password: ${ADMIN_SEED_PASS:$Shashank2003}
    expert-password: ${EXPERT_SEED_PASS:shashi123}
    client-password: ${CLIENT_SEED_PASS:shekhar123}

# Institutional Razorpay Config
razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:placeholder}
    secret: ${RAZORPAY_KEY_SECRET:placeholder}

# Supabase Cloud Storage Config
supabase:
  url: ${SUPABASE_URL:placeholder}
  key: ${SUPABASE_KEY:placeholder}
  bucket: ${SUPABASE_BUCKET:legal-docs}

logging:
  level:
    root: INFO
    com.LawEZY: INFO
